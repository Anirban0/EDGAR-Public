package edgar.domain

/** Scaffold marker for the `domain` module (case classes, IDs, errors — zero deps).
  * Real domain types (`FilingRef`, status enum) arrive with roadmap task 1.1.
  */
object Module:
  val name: String = "domain"
