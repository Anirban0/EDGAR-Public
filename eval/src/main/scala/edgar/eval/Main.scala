package edgar.eval

/** RAG eval harness entry point (`sbt eval/run`).
  * Golden-set runner (recall@8, citation coverage, faithfulness) lands with roadmap task 5.2.
  */
object Main:
  def main(args: Array[String]): Unit =
    println("edgar eval harness — golden set runner not yet implemented (roadmap 5.x)")
